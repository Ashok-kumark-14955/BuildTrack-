# Production environment for Catalyst Slate build
# VITE_API_BASE is the construction-api Function URL (no trailing slash).
# Catalyst Slate and the Function share the same project domain, so
# we use an absolute URL here so the browser calls the Function directly.
VITE_API_BASE=https://project-rainfall-60081725173.development.catalystserverless.in/server/construction-api
